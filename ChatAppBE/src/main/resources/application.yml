app:
  name: ChatAppBE
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
rabbitmq:
  host: localhost
  virtualhost: /
  username: guest
  password: guest